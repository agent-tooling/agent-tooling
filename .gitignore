# Secrets — never commit environment files or credential-bearing configs.
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
credentials.json
secrets.json

# Build / runtime artifacts common to every project in this org.
node_modules
dist
build
coverage
.next
.turbo
.pnpm-store

# Local database / cache files.
.pglite
.data
.data.*

# OS / editor cruft.
.DS_Store
*.log
.idea
.vscode/*.log
